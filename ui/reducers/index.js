// import { FETCH_CLIENTS, FETCH_CLIENTS_SUCCESS } from '../actions/action-types';

const initialState = {
  nomeApp: 'basicApp',
};

const rootReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default rootReducer;
